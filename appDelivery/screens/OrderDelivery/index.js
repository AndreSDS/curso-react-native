import React from 'react';

import {View, Text, StyleSheet} from 'react-native';

const OrderDelivery = () => {
  return <Text style={styles.title}>OrderDelivery</Text>;
};

const styles = StyleSheet.create({
  title: {
    fontWeight: 'bold',
    fontSize: 22,
    color: '#fff',
  },
});

export default OrderDelivery;
